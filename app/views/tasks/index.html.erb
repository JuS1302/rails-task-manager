<div class="container my-5">
  <div class="text-center mb-4">
    <h1 class="display-4">My Tasks</h1>
    <p class="lead">Gérez vos tâches facilement en les cochant une fois terminées.</p>
  </div>

  <div class="row justify-content-center">
    <% @tasks.each do |task| %>
      <div class="col-md-8 mb-3">
        <div class="card shadow-sm">
          <div class="card-body d-flex align-items-center">
            <input type="checkbox" class="form-check-input me-3" <%='checked' if task.completed %>>
            <div class="flex-grow-1">
              <h5 class="<%= task.completed ? 'text-decoration-line-through text-muted' : '' %>">
                <%= link_to task.title, task_path(task), class: "text-dark text-decoration-none" %>
              </h5>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="text-center mt-4">
    <%= link_to "Add a New Task", tasks_new_path, class: "btn btn-warning btn-lg" %>
  </div>
</div>

<style>
  /* Masquer les puces de la liste */
  ul > li {
    list-style: none;
  }

  /* Style pour les cartes */
  .card {
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
  }

  /* Style pour les boutons */
  .btn-lg {
    padding: 0.75rem 1.5rem;
  }
</style>
